# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# Tweaked memory and garbage collector to speed up build processing.
org.gradle.jvmargs=-Xmx4096m -XX:+UseParallelGC -XX:MaxMetaspaceSize=1g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
org.gradle.parallel=true

# AndroidX package structure
android.useAndroidX=true

# Disable Jetifier since all dependencies are migrated/modern
android.enableJetifier=false

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class
android.nonTransitiveRClass=true

# Enable Gradle Build Cache
org.gradle.caching=true

# Enable Gradle Configuration Cache
org.gradle.configuration-cache=true

# Enable File System Watching to speed up incremental builds
org.gradle.vfs.watch=true

# Enable Kotlin incremental compilation
kotlin.incremental=true
# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true
#org.gradle.java.home=C:/Program Files/Android/Android Studio/jbr
